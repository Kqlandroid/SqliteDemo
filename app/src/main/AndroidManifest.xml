<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demos.tags">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".OsChinaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".lanya.MainActivity">

        </activity>
        <activity android:name=".PhoneActivity" />
        <activity android:name=".RecylerViewActivity" />
        <activity android:name=".SqliteListViewActivity" />
        <activity android:name=".CursorAdapterActivity" />
        <activity android:name=".MoreInsertActivity" />
        <activity
            android:name=".VidoPlayerActivity"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden" >

        </activity>
        <activity android:name=".OsChinaActivity" >

        </activity>
        <activity android:name=".BlogActivity" />
        <activity android:name=".DeatilActivity" />
        <activity android:name=".LoginActivity">

        </activity>
        <activity android:name=".slideAdapter.SlideAdapterActivity">

        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>